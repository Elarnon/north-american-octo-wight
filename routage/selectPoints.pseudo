#include "main.h"

void SelectPoints::bestPoints(double lat, double lon, int numElements) {
    // Pour toutes les intersections
    // ajouter dans une liste Pair(inter,
    // distance(inter, parameters))
    //
    // sort la liste en fonction de parameters
    // prendre les premiers numElements
}


private double SelectPoints:: distance(double lat1, double lon1,
        double lat2, double lon2) {
    return pow(lat1 - lat2, 2) + pow(lon1 - lon2, 2)
}
